---
layout: header-image-layout
pageable: true
per_page: 8
---


<ul class="posts">
  <% page_articles.each_with_index do |article, i| %>
    <% 3.times do %>
    <li class="post">
      <div class="post-wrap">
        <h1>
          <%= link_to article.title, article %>
        </h1>
        <h5>
          <%= article.date.strftime('%b %e') %>
        </h5>
        <div class="post-body">
          <%= article.summary %><!-- Requires nokogiri gem --!>
        </div>
      </div>
    </li>
  <% end %>
  <% end %>
</ul>


<% if paginate && num_pages > 1 %>
  <p>
  Page <%= page_number %> of <%= num_pages %>
  </p>

  <% if prev_page %>
    <p>
    <%= link_to 'Previous page', prev_page %>
    </p>
  <% end %>
<% end %>

<% if paginate %>
  <% if next_page %>
    <p><%= link_to 'Next page', next_page %></p>
  <% end %>
<% end %>
